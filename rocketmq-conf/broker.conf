brokerClusterName = DefaultCluster
brokerName = broker-a
brokerId = 0
deleteWhen = 04
fileReservedTime = 48
brokerRole = ASYNC_MASTER
flushDiskType = ASYNC_FLUSH
# 重要：这里设置为宿主机IP，确保消息发送时使用正确的地址
brokerIP1=127.0.0.1
# 重要：这里使用容器名而不是IP地址，确保在容器内可以正常解析
namesrvAddr=rmqnamesrv:9876
# 允许自动创建主题
autoCreateTopicEnable=true